<div class="row">
  <div class="small-12 columns tab_row">
    <dl class="tabs" data-tab>

      {{#link-to 'organisations.detail' model.id tagName="dd" classNames="small-3 columns text-center"}}
        {{#link-to 'organisations.detail' model.id}}
          <div> Details </div>
        {{/link-to}}
      {{/link-to}}

      {{#link-to 'organisations.orders' model.id tagName="dd" classNames="small-3 columns text-center"}}
        {{#link-to 'organisations.orders' model.id}}
          <div> Orders </div>
        {{/link-to}}
      {{/link-to}}

      {{#link-to 'organisations.users' model.id tagName="dd" classNames="small-3 columns text-center"}}
        {{#link-to 'organisations.users' model.id}}
          <div> Users </div>
        {{/link-to}}
      {{/link-to}}
    </dl>
  </div>
</div>
